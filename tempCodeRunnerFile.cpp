#include <bits/stdc++.h>
using namespace std;

#define lli long long int
#define mod 998244353

int solution()
{
    int aa,be,ci;
    if ((aa == (be + ci)) || (be == (aa + ci)) || (ci == (be + aa)))
    cout<<"YES"<<endl;
    else
    cout<<"NO"<<endl;
}

int main()
{
    int adi;
    cin >> adi;
    while (adi--)
    {
       solution();
    }
    return 0;
}
